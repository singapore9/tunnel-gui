if [ "$EUID" -eq 0 ]
  then echo "Please do not run this script as Root! Use it as the pi or dietpi user"
  exit
fi

pip install -r https://github.com/singapore9/tunnel-gui/raw/test-development/requirements.txt

wget https://github.com/singapore9/tunnel-gui/archive/refs/tags/test-development.zip - "tunnel-gui_test-development.zip"
unzip ./tunnel-gui_test-development.zip
rm ./tunnel-gui_test-development.zip
cd ./tunnel-gui-test-development

echo "Succesfully installed the Tunneling GUI. Check the next steps in README.org file"


